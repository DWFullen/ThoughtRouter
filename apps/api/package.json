{
  "name": "@thoughtrouter/api",
  "version": "0.1.0",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts || true",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.1.0",
    "@thoughtrouter/ai": "workspace:*",
    "@thoughtrouter/config": "workspace:*",
    "@thoughtrouter/db": "workspace:*",
    "@thoughtrouter/domain": "workspace:*",
    "@thoughtrouter/github": "workspace:*",
    "@thoughtrouter/testing": "workspace:*",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.1"
  }
}
